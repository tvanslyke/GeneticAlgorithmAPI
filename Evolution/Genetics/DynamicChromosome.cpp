/*
 * DynamicChromosome.cpp
 *
 *  Created on: Apr 6, 2017
 *      Author: tim
 */

#include "DynamicChromosome.h"
/*

using value_type = typename DynamicChromosome::value_type;
using size_type = typename DynamicChromosome::size_type;
using difference_type = typename DynamicChromosome::difference_type;
using reference = typename DynamicChromosome::reference;
using const_reference = typename DynamicChromosome::const_reference;
using pointer = typename DynamicChromosome::pointer;
using const_pointer = typename DynamicChromosome::const_pointer;
using iterator = typename DynamicChromosome::iterator;
using const_iterator = typename DynamicChromosome::const_iterator;
using reverse_iterator = typename DynamicChromosome::reverse_iterator;
using const_reverse_iterator = typename DynamicChromosome::const_reverse_iterator;



DynamicChromosome::DynamicChromosome(size_t len): genes_(len)
{

}
DynamicChromosome::DynamicChromosome(std::initializer_list<Gene> initList): genes_(initList)
{

}

reference DynamicChromosome::operator[](size_type i)
{
	return genes_[i];
}

const_reference DynamicChromosome::operator[](size_type i) const
{
	return genes_[i];
}

reference DynamicChromosome::at(size_type i)
{
	return genes_[i];
}

const_reference  DynamicChromosome::at(size_type i) const
{
	return genes_[i];
}

reference DynamicChromosome::front()
{
	return genes_.front();
}



const_reference DynamicChromosome::front() const
{
	return genes_.front();
}

const_reference DynamicChromosome::back() const
{
	return genes_.back();
}

reference DynamicChromosome::back()
{
	return genes_.back();
}

iterator DynamicChromosome::begin()
{
	return genes_.begin();
}


iterator DynamicChromosome::end()
{
	return genes_.end();
}

const_iterator DynamicChromosome::begin() const
{
	return genes_.begin();
}

const_iterator DynamicChromosome::end() const
{
	return genes_.end();
}


const_iterator DynamicChromosome::cbegin() const noexcept
{
	return genes_.cbegin();
}

const_iterator DynamicChromosome::cend() const noexcept
{
	return genes_.cend();
}

reverse_iterator DynamicChromosome::rbegin() const noexcept
{
	return genes_.rbegin();
}

reverse_iterator DynamicChromosome::rend() const noexcept
{
	return genes_.rend();
}

const_reverse_iterator DynamicChromosome::crbegin() const noexcept
{
	return genes_.crbegin();
}

const_reverse_iterator DynamicChromosome::crend() const noexcept
{
	return genes_.crend();
}

bool DynamicChromosome::empty() const noexcept
{
	return genes_.empty();
}

size_type DynamicChromosome::size() const noexcept
{
	return genes_.size();
}


size_type DynamicChromosome::capacity() const noexcept
{
	return genes_.capacity();
}

void DynamicChromosome::clear()
{
	genes_.clear();
}

void DynamicChromosome::push_back(const Gene & ev)
{
	genes_.push_back(ev);
}


iterator DynamicChromosome::erase(const_iterator pos)
{
	return genes_.erase(pos);
}

iterator DynamicChromosome::erase(const_iterator first, const_iterator last)
{
	return genes_.erase(first, last);
}

void DynamicChromosome::pop_back()
{
	genes_.pop_back();
}

void DynamicChromosome::resize( size_type count )
{
	genes_.resize(count);
}

void DynamicChromosome::resize( size_type count, const value_type& value )
{
	genes_.resize(count, value);
}

*/


